<div class="songs m-3">
<h1>Playlists</h1>
  <div class="row">
  <% @playlists.each do |playlist| %>
      <div class="col-2">
        <%= render partial: "shared/playlist_head" , locals: { playlist: playlist } %>
      </div>
  <% end %>
  </div>
</div>
 

